# Atelier/1 core Python deps (Python 3.12; see docs/STACK.md for why).
# GPU model servers (llama.cpp Vulkan, ComfyUI-Zluda) are installed SEPARATELY as native
# Windows processes - they are NOT pip deps.
#
# IMPORTANT install order on this AMD box: install CPU-only torch FIRST so nothing drags in CUDA:
#   pip install "torch~=2.8.0" "torchaudio~=2.8.0" --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

# --- control plane ---
discord.py>=2.6,<3.0

# --- orchestration (MIT core ONLY; do NOT add langgraph-api / LangGraph Platform) ---
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
aiosqlite>=0.20         # AsyncSqliteSaver inside the bot's event loop (sync saver would block it)

# --- model gateway / LLM client ---
openai>=1.40            # used only as an OpenAI-compatible client against the LOCAL gateway

# --- tts ---
kokoro-onnx>=0.5        # CPU narration (Apache-2.0 voices); model files under models/tts/
soundfile>=0.12

# --- captions ---
faster-whisper>=1.2     # word timestamps on CPU; whisperx (forced alignment) comes with the captions worker

# --- assembly ---
moviepy>=2.0            # NOTE 2.x API: "from moviepy import ..." (moviepy.editor is gone)

# --- diagrams / motion ---
manim>=0.19             # Community Edition; encodes via PyAV, no system ffmpeg needed
matplotlib>=3.8
# depth-anything-v2 is run via its own repo/weights, not pip - see STACK.md

# --- memory / retrieval ---
sqlite-vec>=0.1
sentence-transformers>=3.0   # bge-m3 / bge-small on CPU

# --- research tools ---
httpx>=0.27
SPARQLWrapper>=2.0      # Wikidata (CC-0)

# --- publishing (used from MVP publisher onward) ---
google-api-python-client>=2.130
google-auth-oauthlib>=1.2

# --- observability (v1; not installed during Phase 0) ---
# traceloop-sdk>=0.30   # OpenLLMetry (OTel); view in Phoenix

# --- config / dev ---
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
ruff>=0.5
pytest>=8.2
