"""Entrypoint for the Atelier app process (discord.py gateway + LangGraph orchestrator).

Launched by start-day.ps1 AFTER the GPU model servers are health-gated green.
Phase 0: this is a stub. See tasks/ledger.jsonl (TASK-004, TASK-005, TASK-007).
"""


def main() -> None:
    raise NotImplementedError(
        "Phase 0 stub. Wire up discord.py bot + LangGraph StateGraph. See CONTEXT.md."
    )


if __name__ == "__main__":
    main()
